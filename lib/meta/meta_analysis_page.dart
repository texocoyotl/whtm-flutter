import 'package:flutter/material.dart';

class MetaAnalysisPage extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Meta Analysis'),
    );
  }
}
