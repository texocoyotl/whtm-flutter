import 'package:flutter/material.dart';

class MetaDetailsPage extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Meta Details'),
    );
  }
}
