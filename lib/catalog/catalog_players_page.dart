import 'package:flutter/material.dart';


class CatalogPlayersPage extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Players Catalog'),
    );
  }
}
